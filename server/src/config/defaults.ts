import { IConfig } from "@src/types/common";

const config: IConfig = {
  page: 1,
  limit: 10,
  totalItems: 0,
  sort_type: "desc",
  sort_by: "updatedAt",
  search: "",
  color: "",
  size: "",
  status: "",
};

export default config;
